# 29차 — 상세페이지 품질을 끌어올리기 위한 대규모 리서치 지시서

## 배경 (사용자 원문)

> "상세페이지 커서한테 학습을 엄청나게 시켜서 발전을 시켜야하는데 학습을 엄청나게 좀 할 수 있는
> 지시문까지 줘봐"

지금까지는 문제가 하나 생길 때마다(글래스 배경, 카피 길이, 이미지 합성 등) 그때그때 브리프를
써서 좁게 고쳐왔다. 이번엔 반대 방향 — **문제를 기다리지 않고, Cursor가 스스로 잘 만든
상세페이지들을 대량으로 조사해서 우리 시스템에 뭐가 부족한지 먼저 찾아내고, 구조적으로
끌어올리는** 작업이다.

이미 저장소 안에 이 작업의 1차 결과물이 있다 — `review/reference-patterns.md`. 2026-08-14~15에
외부 상세페이지 4곳을 분석해서 만든 문서인데, **화장품/뷰티와 전자기기 카테고리에만** "카테고리
구조 패턴"(§10)이 있고, 나머지(의류/패션, 식품, 생활용품, 반려동물)는 비어 있다. 이번 라운드는
이 문서를 **훨씬 더 크게, 전 카테고리로** 확장하는 작업이라고 생각하면 된다.

## 절대 규칙 — 아무리 리서치를 많이 해도 이건 넘지 않는다

`review/reference-patterns.md` §10에 이미 있는 규칙을 그대로 유지·확대 적용한다:

1. **브랜드명·세일즈 카피·슬로건을 그대로 베끼지 않는다.** 참고하는 건 오직 **배치 구조**
   (섹션 순서, 텍스트/이미지 비율, 여백 리듬, 정보 위계) — 문장이나 워딩이 아니다.
2. **후기·채팅형 리뷰·인증 배지·QC 그리드는 만들지 않는다.** 우리 슬롯에 없는 섹션 타입이고,
   전부 사실이 아닌 걸 지어내게 되는 영역이라 아예 손대지 않는다 (`lib/section-templates.ts`에
   새 슬롯 타입을 만드는 것도 이번 라운드 범위 밖 — 발견해도 "제안"으로만 남기고 별도 승인
   없이 구현하지 않는다).
3. **없는 수치·스펙·임상 데이터를 만들지 않는다.** `spec_table`/`comparison_table`은 입력값에
   실제 숫자가 있을 때만 채운다는 기존 규칙 그대로.
4. **슬롯 순서·종류(`lib/section-templates.ts`)는 리서치만으로 바꾸지 않는다.** 바꿀 필요가
   있다고 판단되면, 이유와 근거를 정리해서 **먼저 제안하고 승인받은 뒤에** 구현한다 (지금까지
   해온 브리프 방식 그대로 — Claude가 재검증하는 절차를 건너뛰지 않는다).
5. **디자인 토큰(`lib/design-tokens.ts`)의 3색 규칙·히어로-only 장식 규칙**은 이미 검증된
   시스템 제약이다. 레퍼런스가 아무리 화려해도 이 제약 밖으로 나가는 연출(예: 히어로 밖 장식,
   네이비 솔리드 풀폭 밴드 같은 것)은 흉내 내지 않는다.

## 리서치 범위 — 뭘 조사할지

### A. 카테고리별 구조 패턴 보강 (최우선)

`review/reference-patterns.md` §10에 아직 없는 카테고리부터 채운다:

- **의류/패션** — 무신사, 지그재그, 에이블리 등에서 실제 판매량 상위 상세페이지 5곳 이상
  스크린샷 분석. 사이즈 표기 방식, 코디컷 배치, 소재 설명 위치, 색상 옵션 노출 방식.
- **식품/건강기능식품** — 마켓컬리, 오늘의집 푸드, 쿠팡 로켓프레시 상위 상품. 원산지·영양정보
  강조 방식, "왜 이 성분인가" 설득 구조, 섭취 방법 안내 톤.
- **생활용품** — 오늘의집, 스마트스토어 생활용품 베스트. 사용 시나리오 컷 배치, 소재/내구성
  설명 방식.
- **반려동물** — 지금 전용 슬롯 템플릿이 없고 `생활/리빙`(HOME_FALLBACK)으로 폴백되고 있다
  (`lib/section-templates.ts` 603~611행 `CATEGORY_TO_TEMPLATE`). 반려동물 전용 상세페이지들을
  조사해서, 정말 전용 템플릿이 필요한 카테고리인지 아니면 지금 폴백으로 충분한지부터 판단해서
  근거와 함께 제안할 것.
- **전자제품/화장품·뷰티** — 이미 있는 §10 표를 2026-08-15 이후 최신 사례로 갱신·보강.

각 카테고리마다 최소 3~5개의 **실제로 존재하는, 스크린샷으로 근거를 남길 수 있는** 레퍼런스를
쓴다. 접속이 안 되면 §8처럼 URL만이라도 남기고 캡처 근거를 우선한다.

### B. 카테고리 횡단 리서치 (구조·설득 원칙)

- **2026년 기준 이커머스 CRO/UX 리서치**: 스크롤 전환율, F/Z 패턴 시선 흐름, "One Big Point per
  Section" 같은 원칙이 최신 데이터로도 여전히 유효한지, 아니면 업데이트할 부분이 있는지.
  (`review/reference-patterns.md` §8.3의 "한 스크롤 깊이에 주장 하나" 원칙이 근거다.)
- **와디즈/텀블벅류 크라우드펀딩 상세페이지**: 정보 밀도가 매우 높은 페이지들이라, 우리
  `spec_table`/`comparison_table`/`caution` 같은 정보형 슬롯의 레이아웃 참고에 유용할 수 있다.
- **해외 Shopify DTC 브랜드 / Amazon A+ Content**: 미니멀 프리미엄 톤(§8.6에 이미 언급된 방향)의
  최신 사례. 특히 여백/타이포 위계 쪽 참고.
- **경쟁 AI 상세페이지 생성 툴**(있다면): 우리와 비슷한 문제(합성 품질, 카피 길이, 슬롯 다양성)를
  어떻게 풀고 있는지 — 베끼자는 게 아니라, 우리가 놓친 실패 케이스가 있는지 확인하는 용도.

### C. 지금 우리 시스템의 약점 재점검

리서치 결과를 실제 코드/토큰과 대조해서, 아래 같은 질문에 답을 남긴다 (예시일 뿐 — 리서치하며
발견하는 대로 추가):

- `lib/section-templates.ts`의 5개 템플릿(화장품/뷰티, 패션/의류, 식품, 전자/가전, 생활/리빙) 중
  실제 상위 상세페이지들과 비교했을 때 슬롯이 부족하거나 순서가 어색한 곳이 있는가?
- `lib/design-tokens.ts`의 3색 규칙이 카테고리별로 정말 충분한가, 아니면 카테고리마다 조금씩
  다른 색상 운용 규칙이 필요한가?
- 카피 길이 규율(27차, `buildSectionLengthGuide()`)처럼 "전 카테고리 공통이어야 하는데 화장품만
  있었던" 패턴이 다른 영역에도 남아있는가? (아이콘 생성, 배경 프롬프트, 장식 로직 등을 카테고리
  기준으로 훑어볼 것.)

## 작업 방식 — 한 번에 다 갈아엎지 말 것

지금까지 해온 방식(문제 하나 → 브리프 하나 → Claude 재검증 → 커밋)을 이번에도 그대로 따른다.
리서치가 크다고 코드 변경까지 한 번에 크게 하지 않는다:

1. **먼저 리서치만 한다.** 카테고리별로 `review/reference-patterns.md`에 §10 스타일 표를
   채워 넣는다 (구조 패턴 / 우리 슬롯·렌더러 매핑 / 하지 말 것 3열 표 형식 유지). 이 단계는
   코드 변경이 없으므로 tsc 걱정 없이 자유롭게 진행 가능.
2. **리서치에서 나온 "이건 코드로 고칠 만하다" 항목만 골라서**, 항목별로 작고 구체적인 수정
   제안을 정리한다 (파일/행 수준까지, 지금까지의 브리프들과 같은 정밀도로). 이 목록을 별도
   섹션(`## 코드 변경 제안 목록`)으로 `review/reference-patterns.md` 맨 아래나 별도 파일에
   정리해서 남겨둔다 — **바로 구현하지 말고, 목록만 만들어서 다음 라운드에 하나씩 브리프로
   쪼개 처리한다.** (한 번에 여러 파일을 동시에 광범위하게 고치면 회귀를 재현하기 어려워진다 —
   지금까지 프로젝트가 지켜온 원칙과 같음.)
3. 제안 목록 중 **명백히 안전하고 범위가 작은 것**(예: 특정 카테고리 프롬프트에 문구 한 줄
   추가, 기존 함수의 카테고리 분기 하나 추가)은 이번 라운드에 바로 구현해도 좋다. 단, 구현한
   건 반드시 `npx tsc --noEmit` 통과 + 정확히 어떤 파일 몇 행을 바꿨는지 보고할 것 — 지금까지와
   동일하게 Claude가 코드 대조·tsc·라이브 E2E로 재검증한다.
4. 슬롯 신설, 템플릿 구조 변경처럼 **범위가 큰 제안은 절대 바로 구현하지 말고** 제안으로만
   남긴다.

## 산출물

- `review/reference-patterns.md` — §10을 전 카테고리로 확장 (의류/패션, 식품, 생활용품,
  반려동물 추가, 화장품·전자기기는 최신 사례로 보강).
- `review/reference-patterns.md` 안에 (또는 신규 파일 `review/upgrade-proposals.md`로 분리해도
  좋음) `## 코드 변경 제안 목록` — 리서치에서 나온, 아직 구현 안 한 아이디어를 우선순위와 함께
  정리.
- 이번 라운드에 실제로 구현한 항목이 있다면, 기존 관례대로 어떤 파일 몇 행을 왜 바꿨는지 보고.
  (Claude가 코드 대조·tsc·필요시 라이브 E2E로 재검증할 것이므로, "완료" 보고만으로 끝내지 말고
  변경 파일 목록을 구체적으로 남겨줄 것.)

## 참고 — 이미 없는 파일

`review/reference-patterns.md` 안에서 `review/CHECKLIST.md`와
`review/backdrop-prompt-templates.md`를 참고 파일로 언급하고 있는데, 확인해보니 저장소에 실제로
**존재하지 않는다.** 리서치하다가 이 파일들을 찾다가 시간 쓰지 말고, 필요하면 이번 기회에
새로 만들거나 참고 목록에서 지워도 된다.
