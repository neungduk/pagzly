# 34차 — 33차에 같이 들어간 `keepOriginal: true` 되돌리기 + 별도 설계안 요청

## 확인된 사실

33차 완료 보고를 코드 대조로 재검증하다가, 보고에는 없던 변경을 하나 더 발견했습니다.

`lib/photo-pipeline-client.ts`의 `enhanceImages()` 메인 루프(현재 320~353행)에 라운드로빈
수정과 함께 다음 줄이 같이 추가돼 있습니다:

```ts
keepOriginal: true,
// 원본 행·파일을 유지해 draft 재승인 시 옛 URL이 살아 있게 한다 (30차 후속).
```

이건 33차 브리프가 요청한 범위(배경 라운드로빈)가 아니고, 완료 보고서 어디에도 언급이
없었습니다. `app/api/enhance-image/route.ts`를 보면 `keepOriginal`이 true/false일 때
동작이 완전히 다릅니다:

- **false(기존, 메인 루프 원래 동작)**: `product_images`의 기존 행을 `storage_path`
  기준으로 찾아 **update**(같은 행이 새 경로/URL로 덮어써짐) + 원본 스토리지 파일
  **삭제**.
- **true(33차에 새로 추가됨)**: 기존 행은 그대로 두고 **새 행을 insert**, 원본 스토리지
  파일도 **삭제하지 않고 그대로 둠**.

즉 지금 코드는 사진 1장을 보정할 때마다 원본 파일+원본 행을 영구히 남기고 보정본 행을
추가로 만드는 구조로 바뀌어 있습니다. 사진 7~10장이면 생성 1회당 DB 행과 스토리지 파일이
거의 2배로 늘어나고, 사용자가 같은 상품을 여러 번 재생성/재승인하면 계속 누적됩니다.

## 왜 되돌리는지

주석의 의도("draft 재승인 시 옛 URL이 살아 있게")는 이해되지만:

1. 33차 브리프 범위 밖의 변경이고, 보고에도 없어서 검증 없이 그냥 넘어갈 뻔했습니다 —
   저희 프로젝트 원칙상 **범위 밖 변경은 먼저 설계안으로 보고**하고 승인 후 구현하는
   방식을 지켜왔습니다.
2. 스토리지/DB 증가량, 30차의 `protected_until`/`cleanup-expired-images`가 이 늘어난
   원본 행을 실제로 언제 정리하는지(또는 영구히 안 지워지는 건 아닌지) 전혀 검증되지
   않은 채 들어갔습니다.
3. "재승인 시 옛 URL이 깨진다"는 문제가 실제로 재현되는 문제인지, 재현된다면 정확히
   어떤 시나리오인지 구체적으로 확인된 바가 없습니다.

## 지시 — 이번엔 두 가지만

### 1. `keepOriginal: true` 되돌리기 (33차 범위만 남기기)

메인 루프의 `enhanceOne(...)` 호출에서 `keepOriginal: true` 줄과 그 주석을 제거해서
33차 이전 동작(update + 원본 삭제)으로 되돌려 주세요. 라운드로빈 관련 코드(`slotIndex`,
`resolvedBackdrop`, `backdropLabel`, `console.log`)는 그대로 둡니다 — 그 부분은 이미
검증 통과했습니다.

### 2. "draft 재승인 시 옛 URL 깨짐" 문제는 별도 설계안으로 (구현 X, 보고만)

다음을 조사해서 **제안만** 해주세요 — 이번 라운드에 구현하지 않습니다:

- 이 문제가 실제로 재현되는 시나리오인지 (재현 스텝 포함). 재현 안 되면 "확인 결과
  실제 문제 아님"으로 결론 내려도 됩니다.
- 재현된다면, `keepOriginal: true`를 매 생성마다 켜는 것 말고 더 좁은 범위의 해법이
  있는지(예: 재승인 직전에만 한시적으로 원본을 보호하거나, draft 단계에서 이미 하고
  있는 `protect-product-images` 호출을 재승인 시점에도 다시 태우는 식) — 스토리지
  증가 없이 해결하는 방법 우선 검토.
- `keepOriginal: true`를 정말 써야 한다면, 늘어난 원본 행이 `cleanup-expired-images`에
  의해 실제로 며칠 안에 정리되는지, 아니면 `product_id`가 끝내 안 붙어서 영구히
  남는지 — DB 실측으로 확인.

## 검증 요청

되돌린 후 `lib/photo-pipeline-client.ts` diff를 정확히 그 두 줄(코드+주석)만 지웠는지
확인 가능하게 보고해 주시고, `tsc --noEmit` 재확인 부탁드립니다. 이번엔 라이브 재생성은
불필요합니다 — 코드 되돌리기만 확인되면 충분합니다.
